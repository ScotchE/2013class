<?php

class PilotesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('pilotes')->truncate();

		$pilotes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('pilotes')->insert($pilotes);
	}

}
